# SOIS Trainee Frontend (FE-1) - environment variables (example)
# Copy to .env.local to override. Never commit .env.local.

# Next.js rewrite proxy target (see next.config.mjs).
# The proxy rewrites /api/:path* to this origin + /api/:path*.
NEXT_PUBLIC_API_URL=http://localhost:3001