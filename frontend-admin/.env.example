NEXT_PUBLIC_API_URL=http://localhost:3001
# Unified with frontend-trainee naming (was NEXT_PUBLIC_API_BASE) — next.config.mjs reads this env, falls back to localhost:3001
# Proxy: /api/v1/* -> ${NEXT_PUBLIC_API_URL}/api/v1/*  (I1.4)
